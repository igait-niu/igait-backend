services:
  socials-test:
    image: ghcr.io/hiibolt/socials:latest
    ports:
      - "4000:4000"
    environment:
      - FOO='BAR'
  backend:
    image: ghcr.io/hiibolt/igait-backend:latest
    ports:
      - "3000:3000"
    volumes:
      - 'data:/data'
      - 'ssh:/root/.ssh'
    env_file:
      - .env)
    environment:
      - FIREBASE_API_KEY='QGqgZ26VvBSjyLpvNtmrAn0zyaBwVOFFa2V7NCFr'
  frontend:
    image: ghcr.io/michaelslice/igait-web:latest
    ports:
      - "4173:4173"
    environment:
      - FOO='BAR'
volumes:
  data:
  ssh:
